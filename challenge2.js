// 1. Start with an array of strings with a mix of uppercase and lowercase letters. Print every word in all lowercase letters.
// AFTER CREATING STRINGS WITH A MIXTURE OF LOWERCASE AND UPPERCASE LETTERS, PRINT ALL STRINGS WITH ONLY LOWERCASE LETTERS


var mixStrings = ['HeLLo', 'GOodBYe', 'ChOCOlate', 'HEllICOPter']

for (let i = 0; i < mixStrings.length; i++) {
  console.log(mixStrings[i].toLowerCase());
};